# Project Instructions

- For substantial programming work, follow `C:\Git\MUST_READ_AGENT_ROUTING.md`.
- Read `PROJECT_STATUS.md` before changing the project and update it before finishing.
- Run: `npm run start:teacher-server`, then open `http://localhost:8787/online`.
- Verify: `node --check src/app.js` and `node --check tools/teacher_sync_server.mjs`.
- Preserve: `Klasser/` contains teacher and student data and must never be committed.
- Do not commit, push, or publish without explicit user authorization.
